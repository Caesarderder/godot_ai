# 制作人 ownership

Choose only the smallest relevant leaf:

- `game-prototype` (stage; activation: `contextual`; owner: `game-prototype`) → [knowledge leaf](../references/indexes/nodes/game-prototype.md)
- `game-vertical-slice` (stage; activation: `contextual`; owner: `game-vertical-slice`) → [knowledge leaf](../references/indexes/nodes/game-vertical-slice.md)
- `game-project-contract` (practice; activation: `contextual`; owner: `game-project-contract`) →
  [knowledge leaf](../references/indexes/nodes/game-project-contract.md). It owns the project-level
  player promise, scope, milestone acceptance, canonical contract state, and drift resolution.

- Other roles own their implementation or evidence fields and route disputed intent back here; they
  do not copy the contract body.

Do not load every item on this page. Cross-role needs must route to the other role's canonical home.
