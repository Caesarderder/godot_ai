---
name: godot-art
description: Use for Godot visual direction, asset discovery or production, UI presentation, animation, VFX, shaders, and 2D or 3D content integration. Do not use for interaction rules, Control/runtime code, audio, or release evidence. Route through one knowledge-map axis and only the smallest relevant leaves.
---

# 美术

本岗位拥有：视觉方向、素材查找与下载、UI 表现、动画、VFX、Shader 与 2D/3D 内容集成。

不负责：交互意图路由策划，Control 与运行时代码路由程序，声音反馈路由音效。

先读 [岗位地图](map/index.md)，只选择 [系统](map/systems.md) 或 [实践](map/practices.md)。
阶段与类型问题路由制作人或策划，不进入空轴。随后只打开一到两个匹配 leaf。只有 leaf 直接
链接时，才读取脚本、场景、模板或资产。

同一能力有 canonical owner 和 `gd-agentic-*` imported leaf 时默认选前者；精确旧名、更窄技术
或 imported leaf 独有工件才选择后者。

视觉工作涉及 accepted 玩家承诺、玩法事件或验收规格时，只读
[美术契约 preflight](map/contract.md) 的相关项；表现微调无契约变化时不启动完整治理。

## 风险与工件升级

选 leaf 前只判断工程成熟度、请求结果、最高风险声明和现有 owner/pattern，不启动问卷。先选
一个 leaf，第二个只解决不同阻塞风险。读完 leaf 的完整 `REFERENCE.md` 后，仅对实现、修复或
具体生产结果，且直接工件命中最高风险、工程又没有同等强的已验证模式时，才按 focused
test/demo、GDScript/scene/resource prototype、template/config、conceptual reference 的顺序打开
最多三个工件。正文要求 adaptation 前审查 prototype 时，至少打开一个，否则记录拒绝全部候选
的具体兼容性或项目适配理由。

编辑前记录 `adapt`、`reject` 或 `guidance-only` 及原因；只读索引不算使用示例。缓存本轮已读
文件，不重复读取未变内容；只跟随 wrapper/current `content_home`。内部 `SKILL.md` 失败时回到
wrapper 和已安装 `REFERENCE.md`，不猜 legacy 路径，不批量打开 scripts bundle。

Do not load every axis, invent a mandatory production workflow, or treat preserved reference-only
material as approved for the Godot 4.6.x GDScript Web Compatibility single-thread target.
