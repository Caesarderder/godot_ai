# Component gallery

Use to make component states visible before wiring business data. Add every new reusable Control to
the gallery with normal, focused, disabled, loading, error, and empty states that apply.

The gallery is not a screenshot-based visual approval. Test it at narrow and wide Web Preview sizes,
with keyboard/controller focus and long localized text.
